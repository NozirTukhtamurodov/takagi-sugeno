# 🔮 Классификатор Такаги-Сугено

## 🚀 Быстрый старт

### Запуск с меню (рекомендуется):
```bash
python run.py -i
```

### Запуск с настройками из config.py:
```bash
python run.py
```

### Быстрый выбор режима:
```bash
python run.py --mode boosting      # Бустинг (рекомендуется)
python run.py --mode ensemble      # Ансамбль
python run.py --mode single        # Одиночная модель
python run.py --mode hierarchical  # Иерархическая
```

### Показать текущие настройки:
```bash
python run.py --config
```

---

## ⚙️ Настройка

Все параметры находятся в файле **`config.py`**:

| Параметр | Описание | Рекомендуемые значения |
|----------|----------|------------------------|
| `N_MFS` | Функции принадлежности | 5-7 |
| `MAX_RULES` | Максимум правил | 200-350 |
| `TEMPERATURE` | Температура softmax | 0.3-0.5 |
| `BOOSTING_ROUNDS` | Раунды бустинга | 5-10 |

---

## 📊 Режимы работы

| Режим | Описание | Когда использовать |
|-------|----------|-------------------|
| 🎯 **Single** | Одна модель | Быстрый тест |
| 👥 **Ensemble** | Несколько моделей | Стабильность |
| 🚀 **Boosting** | Адаптивное обучение | Макс. точность |
| 🏛️ **Hierarchical** | Двухуровневая | Много похожих классов |

---

## 📁 Структура файлов

```
├── run.py              # Точка входа (запускайте этот файл)
├── config.py           # Все настройки в одном месте
├── takagi_sugeno_optimized.py  # Основной код классификатора
├── train2.csv          # Данные для обучения
├── plots_classical/    # Графики классической модели
├── plots_neutrosophic/ # Графики нейтрософской модели
└── plots_comparison/   # Сравнительные графики
```
